# AGENTS.md

このファイルは、AI エージェント（Cursor / Claude Code など）がこのリポジトリで作業するときの共通ルールをまとめたものである。

**このファイルは新規プロジェクトへそのままコピーして使える汎用テンプレートとして書かれている。**
プロジェクト固有の事情（使うコマンド、ディレクトリ構成など）は、最後の「プロジェクト固有設定」にだけ書き、それ以外の章は基本的に書き換えずに使うことを想定している。

- 置き場所: リポジトリ直下（`AGENTS.md`）
- 想定読者: AI エージェント、および新しく参加した開発者
- 迷ったとき: このファイルの記述が、暗黙の慣習より優先される

---

## 目次

- [1. 言語設定](#1-言語設定)
- [2. 文章作成の指針](#2-文章作成の指針)
- [3. 作業の進め方](#3-作業の進め方)
- [4. コメントとドキュメント](#4-コメントとドキュメント)
- [5. コメントタグ](#5-コメントタグ)
- [6. TSDoc 記載ポリシー](#6-tsdoc-記載ポリシー)
- [7. `#region` / `#endregion` の使用ルール](#7-region--endregion-の使用ルール)
- [8. テストコード](#8-テストコード)
- [9. 品質管理と禁止事項](#9-品質管理と禁止事項)
- [10. 開発環境の活用](#10-開発環境の活用)
- [11. プロジェクト固有設定](#11-プロジェクト固有設定)

---

## 1. 言語設定

- AI エージェントは、**常に日本語で応答する**こと。
- コード内のコメント、ドキュメント、コミットメッセージ、PR 本文などは、**そのコードで主に使われている言語に合わせる**。迷った場合は日本語で構わない。

---

## 2. 文章作成の指針

レスポンスやドキュメントを書くときは、次の点に注意する。

- **専門用語を避ける**: 可能な限り専門用語を使わず、理解しやすい言葉で説明する。
- **平易な表現を心がける**: 他の言語を主に使う人でも読みやすい、シンプルで明確な表現にする。
- **具体例を交える**: 抽象的な説明だけで終わらせず、具体例を示して理解を助ける。
- **結論から書く**: 「何が起きたか」「何を変えたか」を最初の 1〜2 文で伝え、理由や詳細はその後に書く。

---

## 3. 作業の進め方

このプロジェクトで実際にうまく回っている進め方を、手順として整理したもの。

### 3.1 実装前に「決めること」を先に潰す

大きめの機能を作るときは、コードを書く前に次の順で整理する。

1. **判断の軸を 1 つ決める**
   実装中に迷ったときに何を優先するかを、あらかじめ 1 行で決めておく。
   （例: 「迷ったら通信量削減に倒す」「迷ったら既存挙動の互換性に倒す」）
   軸が決まっていれば、細かい判断でいちいち止まらずに進められる。
2. **やること / やらないことを両方書く**
   「非実装」の一覧を明示する。あとから「これは入っていないのか」と揉めるのを防ぐ。
3. **曖昧な点を洗い出して確認する**
   仕様が決まっていない点は、勝手に決めずに選択肢を示して確認する。
   確認して決まったことは「決定事項」として表に残す。
4. **実装手順を番号付きで並べる**
   上から順に進められる粒度（1 項目 = 1 コミット程度）に分割する。

### 3.2 計画書（プランファイル）の書き方

計画は Markdown で残す。次の見出しを持たせると、あとから読み返しやすい。

| 見出し     | 書く内容                                       |
| ---------- | ---------------------------------------------- |
| 概要       | 何を作るか、既存のものをどう扱うか（残す/置き換える） |
| 配置と構成 | 追加・変更するファイルと、その役割の表           |
| 方針メモ   | 判断の軸、やらないと決めたこと                   |
| 設計方針   | 基本原則。図が有効なら Mermaid で                |
| 機能一覧   | 機能 → 実装方法 → 使う API / 方式 の対応表       |
| 非実装     | 実装しない項目とその理由                         |
| 決定事項   | 確認して決まったことの表                         |
| 実装手順   | 番号付きの作業リスト                             |

- 参照するファイルは、必ず相対パスへのリンク付きで書く（あとで追いやすくするため）。
- 図は文章の代わりではなく、文章では伝わりにくい流れ（分岐・順序）を補うために使う。

### 3.3 コミットの粒度とメッセージ

- **1 コミット = 1 つの意味のある変更**。無関係な変更を混ぜない。
- メッセージは**日本語 1 行**で、何をしたかが分かるように書く。
  - 例: `〇〇のバグを修正` / `〇〇を追加` / `〇〇するように変更` / `〇〇の表示を修正`
- **明示的な指示がない限り、コミット・プッシュはしない**。
- ライセンスファイル（`LICENSE`、`LICENSE.*` など）の作成・変更は、**明示的な指示があるときだけ**行う。ライセンスが定義されていない場合に、勝手に定義しないこと。

### 3.4 触ったところから少しずつ直す（漸進的改善）

このルールは重要なので、単独で守ること。

> ファイルを触ったら、そのファイルに不足している
> 「処理内容や分岐の意図を説明する行コメント」「`@remarks` を含む `@packageDocumentation`」「TSDoc」「`@public` / `@internal`」「`#region`」
> を、ルールに従って**可能な範囲で補完する**。

- 特に、コメントの無い行が続いている箇所は、読みやすさのために改善する。
- 既存プロジェクトはこうした情報が不足しがちで、一括で直すのは大掛かりになる。**触った所から一つずつ直していくことで、長期的に改善する**という考え方を取る。
- ただし、**今回の変更範囲に自然に含まれる範囲**に留め、大規模で無関係なリファクタリングにはしない。

### 3.5 エラー・不具合への向き合い方

- エラーが出たら、**根本原因を特定して直す**。症状を隠すだけの対処（try-catch で握り潰す、条件分岐で回避するだけ など）はしない。
- どうしても暫定対応にする場合は、`HACK` / `TEMP` タグと理由をコメントに残す。

### 3.6 変更後の確認

- 変更したファイルについて、**型チェック・lint・ビルド**を通す（コマンドは「プロジェクト固有設定」参照）。
- 自分が入れた lint エラーは必ず直す。既存の lint エラーは、必要な場合にだけ直す。
- `README.md` の記述が古くなっていないか確認し、古ければ更新する。

---

## 4. コメントとドキュメント

### 4.1 基本方針

- **日本語コメントを付ける**: その言語に不慣れな人にも伝わるよう、適宜日本語でコメントする。
- **Doc 形式で意図を明記する**: 可能な限り TSDoc / JSDoc 形式で、コードの意図を書く。
- **ファイル先頭の説明**: コメントが使えるすべてのファイルの先頭に、そのファイルの説明と責務を書く。
- **コメントは「なぜ」を書く**: コードを読めば分かることを繰り返さない。意図・前提・制約・トレードオフを書く。

### 4.2 Vue ファイル（`.vue`）の扱い

- ファイル全体の説明は、`<script setup>` の先頭に TSDoc（`/** @packageDocumentation */`）で書く。
- **HTML コメント（`<!-- -->`）をファイル全体の説明やコメントタグに使わない**。HTML コメントはテンプレート内の要素説明にのみ使う。
- テンプレート内の要素に関する注意点も、コメントタグとして書きたい場合は `<script setup>` 側の TSDoc に書く。

---

## 5. コメントタグ

実装中に気づいた未実装・課題・注意点は、**必ず適切なコメントタグとして記録する**。

### 5.1 記録すべき内容

未実装機能 / 今後の構想 / 技術的負債 / 既知の不具合 / 仕様変更点 / 危険な領域 / 暫定実装 / 補足情報 / レビュー依頼 / 疑問点 / 一時的処理 / デバッグ用コード / 最適化余地 / 警告

### 5.2 使用可能なタグ

| タグ       | 意味                     | 使う場面                             |
| ---------- | ------------------------ | ------------------------------------ |
| `TODO`     | 未対応の実装・作業       | 実装予定、追加対応が必要             |
| `FIXME`    | 既知の不具合・危険な挙動 | バグ、誤動作、例外ケース             |
| `BUG`      | バグの指摘（現象）       | 症状の記録、再現条件                 |
| `CHANGED`  | 仕様変更・挙動変更       | 互換性への影響、移行が必要           |
| `XXX`      | 非推奨・危険・要注意     | 触ると壊れる領域、代替案が不明       |
| `IDEA`     | アイデア・提案           | 将来の改善案                         |
| `HACK`     | 苦肉の策・暫定実装       | 仕方なく歪めている                   |
| `NOTE`     | 補足情報                 | 意図、背景、注意点                   |
| `REVIEW`   | レビュー依頼・要確認     | 実装判断に迷いがある                 |
| `NB`       | 注目すべき注意           | 間違えやすい点（簡潔に要点を書く）   |
| `QUESTION` | 疑問点・未確定           | 仕様が不明、判断待ち                 |
| `COMBAK`   | 後で戻る                 | いま深掘りしないが要回収（乱用しない） |
| `TEMP`     | 一時的処理               | 移行期間だけ必要                     |
| `DEBUG`    | デバッグ用               | ログ・計測・分岐                     |
| `OPTIMIZE` | 最適化余地               | 性能改善の余地                       |
| `WARNING`  | 強い警告                 | 重大な落とし穴・破壊的操作           |

- 「あとでやる」は `TODO` に寄せる。`COMBAK` は乱用しない。
- `WARNING` は「何が危険か」と「回避策」を必ず書く。

### 5.3 書く場所

| 対象                       | 書く場所                                                        |
| -------------------------- | --------------------------------------------------------------- |
| 関数・メソッド・composable | TSDoc の `@remarks`。加えて、その行に意味がある場合は行内 `// TAG:` も推奨（行内に書いた場合は `@remarks` にも記載する） |
| ファイル全体               | `@packageDocumentation` の `@remarks`、またはファイル先頭の TSDoc |
| 型定義                     | 型定義の `@remarks`                                             |
| Vue コンポーネント         | `<script setup>` 内の `@packageDocumentation` の `@remarks`      |
| コンポーネント内の要素     | 該当要素の TSDoc / `@remarks`（HTML コメントは使わない）         |

### 5.4 記載例

```typescript
/**
 * アクションアイテムを取得する
 *
 * @remarks
 * TODO: 実際のAPIからデータを取得する機能を実装
 * - 現在はモックデータを使用
 * TODO: キャッシュ機能の追加を検討
 * FIXME: エラーハンドリングの実装が必要（現在はエラーが握り潰されている）
 * OPTIMIZE: 大量データ取得時のパフォーマンス改善が必要
 * NOTE: この関数は非同期で実行されるため、呼び出し側で await が必要
 *
 * @returns アクションアイテムの一覧
 * @public
 */
function getActionItems(): ActionItem[] {
	// TODO: API呼び出しに置き換える必要がある
	// HACK: ライブラリのバグを回避するため、一時的にこの方法を使用
	return mockActionItems;
}
```

```vue
<script setup lang="ts">
/**
 * @packageDocumentation
 *
 * アクションアイテム一覧ページコンポーネント
 *
 * @remarks
 * TODO: フィルター機能のテスト
 * FIXME: ページネーションで大量データを表示するとパフォーマンスが低下する
 * REVIEW: この実装方法で問題ないか確認が必要
 * NOTE: このコンポーネントは SSR と CSR の両方で動作する
 *
 * @public
 */
</script>
```

### 5.5 注意事項

- コメントタグは「何をすべきか」だけでなく、「なぜ必要なのか」「どのような制約があるのか」も書く。
- **実装が完了したコメントタグは削除する。**
- HTML コメント（`<!-- TAG: ... -->`）でコメントタグを書かない。

---

## 6. TSDoc 記載ポリシー

TSDoc が使える **クラス / インターフェース / 型エイリアス / 列挙型 / 名前空間 / 関数 / 変数 / 定数 / コンストラクタ / メソッド / プロパティ / アクセサ / ファイル全体（`@packageDocumentation`）** には、原則として**必ず TSDoc を書く**。

### 6.1 目的

- 仕様（What）だけでなく、**意図（Why）・前提・制約・トレードオフ**が読み取れること
- 実装の「たまたまそうなっている」ではなく、**契約（Contract）**として読めること
- 後から変更する人が、**壊してはいけない境界**を判断できること

### 6.2 最低限の構成

**共通（全要素）**

- 要約 1 行: 何をする / 何であるか
- `@remarks`: 意図、背景、前提、制約、設計判断、既知の落とし穴
- 関連: `@see` / `{@link ...}` で関連 API・仕様への導線
- 公開範囲: `@public` または `@internal` を**どちらか必ず**明示

**関数 / メソッド**

- `@param`: 全引数（省略不可）
- `@returns`: 戻り値（`void` でも意図が書けるなら書く）
- `@throws`: 投げる例外（例外ごとにブロックを分ける）
- `@defaultValue`: デフォルト引数がある場合
- `@example`: 利用者が迷いそうなら最短の例を 1 つ

**クラス / インターフェース / 型**

- ドメイン上の意味（何を表す型か）
- 不変条件（invariant）や制約
- 期待する利用パターン（生成方法、ライフサイクル、非同期前提など）

**列挙型 / 定数**

- 値の意味、取りうる範囲、追加・削除時の互換性への影響

**モジュールスコープの変数**

- 役割（キャッシュ、状態、DI コンテナ、設定値など）
- 変更されるのか / されないのか（`const` でも中身が可変なら明記）

**コンストラクタ**

- 生成に必要な前提、依存（注入するもの）、初期化で行う副作用

**ファイル全体**

- そのモジュールの責務、公開面、依存の方向、設計の境界（= 「このファイルは何のために存在するか」）

### 6.3 「メモ」の書き方

要素に紐づくメモは、必ず `@remarks` に Markdown で書く。

- わざとそうしていない挙動（偶然そうなっている / 保証しない）
- 将来変える予定（TODO / 改善余地）
- パフォーマンス上の理由（キャッシュ、計算量など）
- 互換性のための理由（古い仕様への追従）
- セキュリティ・権限の前提（入力の信頼性など）

公開ドキュメントに出したくない内部事情は `@privateRemarks` を使う。

### 6.4 省略してよいケース

- 完全に自明な再エクスポート（`export { x } from ...` のみ など）
- テスト専用の補助関数（ただし複雑なら書く）
- 1 行で意味が 100% 明確なローカル関数・変数（モジュール外に影響しないもの）

**公開 API・型・外部に影響があるものは省略不可。**

### 6.5 品質ゲート（任意）

- 公開 API: `@public` 必須、可能なら `@example` も必須
- 変更リスクが高いもの: `@alpha` / `@beta` / `@experimental` を付与
- 非推奨化: `@deprecated` に**代替 API**と**移行方法の一言**を必ず書く

### 6.6 タグ早見表

**リリース段階・可視性・契約**
`@public` / `@internal` / `@alpha` / `@beta` / `@experimental` / `@deprecated` / `@sealed` / `@virtual` / `@override` / `@readonly` / `@decorator` / `@eventProperty`

**説明ブロック**
`@remarks` / `@privateRemarks` / `@defaultValue` / `@example` / `@see` / `@throws` / `@inheritDoc` / `@packageDocumentation`

**シグネチャ**
`@param` / `@returns` / `@typeParam`

**インライン**
`{@link ...}` / `{@label ...}`

---

## 7. `#region` / `#endregion` の使用ルール

コードの見通しを良くするため、`#region` / `#endregion` で構造を明確にする。新規コードだけでなく、既存コードを修正する際にも、十分に長く読みづらい場合は整理対象とする。

### 7.1 基本方針

- エリア名は**日本語**で書く。
- 処理やメンバーのまとまりが明確になる場合に使う。
- **可読性向上のために使い、単に長いコードを隠すために使わない。**
- 本来はメソッド分割・クラス分割すべき箇所を、`#region` だけで済ませない。

### 7.2 使用の目安

次のいずれかに当てはまる場合は使用を検討する。

- クラス全体が 150 行以上になる
- 10 個以上のメンバー（フィールド、プロパティ、メソッドなど）を持つ
- public メソッド、private ヘルパー、イベント処理、変換処理など、責務の異なる要素が混在している
- スクロールしないと全体構造を把握しづらい

※ 目安であり、短くても構造整理が有効なら使ってよい。逆に、長くても単純で読みやすい場合は無理に使わなくてよい。

### 7.3 命名例

`#region 公開メソッド` / `#region 非公開ヘルパー` / `#region イベント処理` / `#region 入力チェック` / `#region 変換処理` / `#region API応答組み立て` / `#region 初期化` / `#region 定数` / `#region フィールド` / `#region プロパティ`

### 7.4 非推奨

- 曖昧な名前（`その他`、`雑多`、`一時`、`各種処理` など）
- 1 つのメソッドだけを囲うための `#region`
- 過度に細かい分割
- 深い入れ子

### 7.5 既存コードの扱い

既存コードを修正する際、対象クラスやファイルが長く構造が把握しづらい場合は、**修正箇所に関連する範囲で** `#region` を追加して整理してよい。ただし大規模な無関係リファクタリングにはせず、今回の変更範囲に自然に含まれる範囲で行う。

---

## 8. テストコード

### 8.1 方針

- 正常系・異常系・境界値を網羅する。
- 外部 API は必ずモック化する。
- モックの戻り値は型定義に従って正確に作る。
- テストケース名は「〜の場合、〜になる」形式で書く。
- setup / teardown で DB や共有状態をリセットする。

### 8.2 テンプレート

言語に合わせた修正は必要だが、可能な限りこの形に沿って埋める。

```typescript
describe("【対象クラス名】", () => {
	beforeEach(() => {
		// 初期化処理
	});

	afterEach(() => {
		// クリーンアップ
	});

	describe("【メソッド名】", () => {
		it("正常系：〜の場合、〜を返す", () => {
			// Arrange
			// Act
			// Assert
		});

		it("異常系：〜の場合、エラーをthrowする", () => {});

		it("境界値：〜", () => {});
	});
});
```

---

## 9. 品質管理と禁止事項

### 9.1 守ること

- **既存のコードスタイル・規約に従う**。周囲のコードと同じ書き方・命名・コメント密度に合わせる。
- **DRY 原則**: 重複コードがないか確認し、共通化できる箇所は積極的に共通化する。
- **TODO の管理**: 未実装機能や改善点をコメントタグで記録し、定期的に見直して状況を更新する。
- **ドキュメントの更新**: `README.md` の内容を常に確認し、古くなっていれば更新する。

### 9.2 しないこと

- 指示なしのコミット・プッシュ
- 指示なしのライセンスファイル作成・変更
- 症状だけを隠す対処（根本原因を直さない修正）
- 今回の変更に関係のない大規模リファクタリング
- HTML コメントによるコメントタグの記載
- 秘密情報を含むファイル（`.env`、認証情報など）のコミット

---

## 10. 開発環境の活用

VSCode を前提環境として想定し、次の機能が活きる書き方を心がける。

- **Doc による型補完**: TSDoc / JSDoc を活用し、補完とホバー説明が効くようにする。
- **設定ファイルへのコメントとサンプル**: 設定ファイルには、意図が分かるコメントとサンプル値を添える。
- **公式拡張との相性**: Volar、ESLint、Prettier など公式拡張が活きる書き方にする（フォーマッタと戦わない）。

---

## 11. プロジェクト固有設定

> ここだけをプロジェクトごとに書き換える。新規プロジェクトにコピーしたら、まずこの章を埋める。

### 11.1 判断の軸

このプロジェクトで迷ったときに優先する方針を書く。

- （例）迷ったら通信量削減に倒す
- （例）迷ったら既存挙動との互換性に倒す

### 11.2 構成

| 項目             | 内容                                       |
| ---------------- | ------------------------------------------ |
| パッケージ管理   | （例）pnpm ワークスペース                  |
| 主なディレクトリ | （例）`packages/backend`、`packages/client` |
| 言語・主要技術   | （例）TypeScript / Vue 3 / PostgreSQL      |

### 11.3 よく使うコマンド

```sh
# 依存関係の導入
# 例: pnpm install

# ビルド
# 例: pnpm build

# 開発サーバー
# 例: pnpm dev

# lint / フォーマット
# 例: pnpm lint

# テスト
# 例: pnpm test
```

### 11.4 注意点

- （例）`built/` 以下は生成物なので直接編集しない
- （例）マイグレーションを追加したら必ず上下両方向を確認する
